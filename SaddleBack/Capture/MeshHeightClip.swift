// Obsolete: the retro-migration that used this has been removed. The grass fix is
// a capture-time height clip in DepthGridMesh, and the raw shot (RawShotWriter) is
// now saved so archive-wide reprocessing can re-run the full build instead.
//
// TODO (Michael): delete this file from the Xcode project (Right-click → Delete →
// Move to Trash). Left blank to avoid editing project.pbxproj from here.
